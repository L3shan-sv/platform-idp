replicaCount: 2
autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 5
env:
  ENVIRONMENT: staging
  LOG_LEVEL: INFO
ingress:
  hosts:
    - host: nerve.staging.internal
      paths:
        - path: /
          pathType: Prefix
