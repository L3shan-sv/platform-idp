apiVersion: v1
kind: Service
metadata:
  name: {{ include "nerve-gateway.fullname" . }}
  labels:
    {{- include "nerve-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nerve-gateway.selectorLabels" . | nindent 4 }}
